export type Colors_react_im_index_confetti = {
    order: number
    configs: { confettiNumber: number }
}

export type Emojis_react_im_index_confetti = {
    order: number
    configs: {
        emojis: string[]
        confettiNumber: number
    }
}
